# work

## "never rate-limited" promises more than the cadence can keep

- ask: you asked how confident we are that the download rate wouldn't get a user limited. Honest read: the *pacing* is sound — an 8–20s random gap before each video, deliberately slow, right for the download load — but the word "never" overclaims. The README header says "Never rate-limited" and this project's graph check says "never trips a rate limit"; neither is keepable. What trips YouTube's bot-check in 2025 is mostly login state and IP reputation, not the gap between videos: a logged-out bulk run can still draw the "confirm you're not a bot" 429 however slowly it sleeps, and ytgrab runs logged-out (no cookie support). The other soft spot is the start-of-run metadata burst — enumerating the whole channel before the oldest video — paced only by the 1s request gap, not the inter-video sleep. Net: low risk of a hard ban at this cadence, a real chance of an occasional 429 on a large channel, and shipped wording that promises more than we can deliver. How to close the gap is your decision.
- options: Soften the claim only — reword the README "Never rate-limited" header and the graph's "never trips a rate limit" to honest pacing language ("paced to stay polite; can't promise never"), no code change; honest at once, ships no new behavior . Harden to earn it — add real rate-limit backoff (pause and lengthen, not just retry), throttle the start-of-run enumeration, add optional browser-cookie auth so runs aren't logged-out, then word the README to match; materially raises confidence, but more code and cookies mean trusting the tool with your YouTube session . Both, staged — soften the wording now so nothing false ships, and file the hardening as its own slice to accept later
- state: awaiting your decision — pick how to close the gap between "Never rate-limited" and what the cadence can actually promise
- since: 2026-06-14
- blocks: nothing in the tool breaks meanwhile — it downloads fine at this cadence. What stands unbacked is the wording: the README header and the graph check both say "never," and that overclaim ships uncorrected until you pick. The risk is reputational (a user trusts "never," then meets a 429), not a crash. Nothing edited yet — direction is yours.

## the zero-download run — your yt-dlp is too old to read YouTube

- ask: you ran ytgrab against @whyalexandery, it saved nothing, and you said the channel definitely has videos. You're right — it does, and the tool isn't really claiming otherwise. "The channel yielded no videos" is your local yt-dlp returning empty: it is 2024.04.09 (fourteen months old as of today), and YouTube has changed enough since that this version reads channels as empty without erroring — the exact failure the run's warning names and this project's graph flagged as the root cause back on 2026-06-13. Both your runs prove it is not the URL: the @handle form (.../@whyalexandery/videos) and the full-URL form (.../@whyalexandery) both yielded zero, and the archive at ~/Videos/.ytgrab/ is empty. The fix is one line — `pip install -U yt-dlp` — after which the channel should enumerate and download oldest-first. Updating yt-dlp touches your environment (it leaves this project folder), so unlike the ffmpeg install you asked for, this one is your call: who runs it and how.
- options: Update it for me now — run `pip install -U yt-dlp` (reversible with `pip install yt-dlp==2024.4.9`), then re-run against @whyalexandery to confirm videos actually land before I trust it; fastest unblock, my recommendation . I run it myself — `pip install -U yt-dlp`, then click or run again; environment changes stay in my hands . Make ytgrab self-heal — on a zero-yield run it offers to update yt-dlp before warning; a change to the tool, more code, and some users won't want a downloader that updates its own dependencies
- state: awaiting your decision — your yt-dlp 2024.04.09 reads YouTube empty; pick how it gets updated (the channel isn't empty, the extractor is stale)
- since: 2026-06-14
- blocks: until yt-dlp updates, every run yields nothing — the tool runs cleanly but the stale extractor downloads zero, so your testing is blocked here exactly as it was on the missing ffmpeg. The diagnosis is certain: version 2024.04.09 + empty archive + both URL forms returning zero. Nothing installed yet.
